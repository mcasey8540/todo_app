<div class="navbar">
  <div class="navbar-inner">
    <%= link_to "Todo App", root_path, class: "brand" %>
    <% if user_signed_in? %>
    <div class = "pull-right">Signed in as <%= current_user.email %>. Not you? <div class="btn"><%= link_to "Sign Out", destroy_user_session_path, method: :delete %> </div></div>
    <% end %>
  </div>
</div>